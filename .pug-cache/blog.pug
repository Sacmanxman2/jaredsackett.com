article.postlist
    ul
        |   {% for post in site.categories.blog %}
        li
              a(href='{{ post.url }}')
                    .postdate {{ post.date | date: "%B %d, %Y" }}
                    h2 {{ post.title }}
                    p {{ post.excerpt }}
        |   {% endfor %}

